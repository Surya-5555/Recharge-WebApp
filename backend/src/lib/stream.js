// Placeholder stream service
const upsertStreamUser = async (userData) => {
  console.log('Stream user operation:', userData);
  return Promise.resolve();
};

module.exports = {
  upsertStreamUser
};